<Window x:Class="Examen_Opdracht_.NET_ADVANCED.Panels.ViewCar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Examen_Opdracht_.NET_ADVANCED.Panels"
        mc:Ignorable="d"
        Title="ViewCar" WindowState="Maximized">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ListBox SelectionChanged="lbCars_SelectionChanged" Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Grid.ColumnSpan="2" SelectionMode="Single" Name="lbCars" HorizontalAlignment="Left" VerticalAlignment="Center" Width="300" Height="500" Margin="30">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock >
                            <Run Text="Make: "/>
                            <Run Text="{Binding Make}" FontWeight="Bold" FontStyle="Italic"/>
                        </TextBlock>

                        <TextBlock >
                            <Run Text="Model: "/>
                            <Run Text="{Binding Model}" FontWeight="Bold" FontStyle="Italic"/>
                        </TextBlock>

                        <TextBlock >
                            <Run Text="License Plate: "/>
                            <Run Text="{Binding LicensePlate}" FontWeight="Bold" FontStyle="Italic"/>
                        </TextBlock>

                        <TextBlock >
                            <Run Text="Customer Name: "/>
                            <Run Text="{Binding Customer.Name}" FontWeight="Bold" FontStyle="Italic"/>
                        </TextBlock>
                    </StackPanel>


                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Column="2" Grid.ColumnSpan="2" Content="Create New Car" BorderBrush="Green" Background="Green" HorizontalAlignment="Center" VerticalAlignment="Top" Width="250" Height="80" Margin="20" Click="btnCreateCar_Click"/>

        <Grid Grid.Column="2" Grid.ColumnSpan="3" Grid.Row="1" Grid.RowSpan="2" Name="createForm" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" FontWeight="Bold" >Make:</Label>
            <TextBox Grid.Row="0" Grid.Column="1" Name="txtMake" HorizontalAlignment="Right" VerticalAlignment="Top" Width="220" Margin="0,20"/>

            <Label Grid.Row="1" Grid.Column="0" FontWeight="Bold">Model:</Label>
            <TextBox Grid.Row="1" Grid.Column="1" Name="txtModel" HorizontalAlignment="Right" VerticalAlignment="Top" Width="220" Margin="0,20"/>

            <Label Grid.Row="2" Grid.Column="0" FontWeight="Bold">LicensePlate:</Label>
            <TextBox Grid.Row="2" Grid.Column="1" Name="txtLicensePlate" HorizontalAlignment="Right" VerticalAlignment="Top" Width="220" Margin="0,20"/>

            <Label Grid.Row="3" Grid.Column="0" FontWeight="Bold">ChassisNumber</Label>
            <TextBox Grid.Row="3" Grid.Column="1" Name="txtChassisNumber" HorizontalAlignment="Right" VerticalAlignment="Top" Width="220" Margin="0,20"/>

            <Label Grid.Row="4" Grid.Column="0" FontWeight="Bold">Customer ID</Label>
            <ComboBox Name="cmbCustomer" Grid.Row="4" Grid.Column="1" Width="220" Margin="0,20" DisplayMemberPath="Name" SelectedValuePath="CustomerId"/>

            <TextBlock Grid.Row="5" Name="txtError" Text="" Foreground="Red" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10"/>

            <Button Grid.Column="1" Grid.Row="6" Content="Save Car" HorizontalAlignment="Center" VerticalAlignment="Top" Width="100" Height="30" Margin="0,10" Click="btnSaveCar_Click"/>
        </Grid>
    </Grid>
</Window>
